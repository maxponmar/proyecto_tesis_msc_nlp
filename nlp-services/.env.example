# =============================================================================
# NLP Services Configuration
# Copy to .env and adjust for your environment
# =============================================================================

# --- FreeLing Service ---
FREELING_HOST=0.0.0.0
FREELING_PORT=4000
FREELING_SERVER_PORT=50005
FREELING_CONFIG=es.cfg
FREELING_TEMP_DIR=temp

# --- BERT Service ---
BERT_HOST=0.0.0.0
BERT_PORT=8000

# Base directory where model folders (QUE/, PARAQUE/, COMO/) live
# Each must contain: data/, results/, pretrainedmodel<FOLDER>/
BERT_MODELS_BASE=/root/nlp-services/models

# Temp directory for per-request working files (auto-created)
BERT_TEMP_DIR=/tmp/bert-workdir

# Path to the labels directory (committed in repo)
BERT_LABELS_DIR=../labels

# Path to NER scripts (auto-resolved relative to bert/)
BERT_NER_SCRIPTS_DIR=ner
